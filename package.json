{
  "name": "hours-tracker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest",
    "test:e2e": "playwright test",
    "test:ci": "npx playwright install && vitest --run && playwright test --reporter=list"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "install": "^0.13.0",
    "vitest": "^1.0.0"
  }
}
